# AIゲーム実験室（ad-games）

スマホ広告でよく見るゲームを、AIだけで作る実験。公開先は https://machino-ai.jp/ad-games/ 、リポジトリは https://github.com/mitsukenn/ad-games 。

## このリポジトリが原本

**両方のゲームとも、直すのはこのリポジトリのファイルだけ。** どのPCからでも、clone して直して push する。

- 4th PC の `073_Webアプリを作成したい/40_広告ゲーム_自分が向かう/` は、Million March を作っていたときの古い作業場所。**参照専用**で、ここを直しても公開には反映されない（2026-09-23にこのリポジトリへ一本化）
- 別のPCで始めるときは `docs/別PCで作業する手順.md`

## ファイル

| ファイル | 中身 |
|---|---|
| `index.html` | 実験室のトップページ（読みもの。迎撃ロードを大きく紹介） |
| `select.html` | ゲームを選ぶ画面。各ゲームのタイトル・一時停止から戻ってくる |
| `geigeki_road.html` | 迎撃ロード（3レーン防衛）。1ファイル完結 |
| `million_march.html` | Million March（行進ランナー）。1ファイル完結 |
| `ranking-gas.js` | ランキングのサーバー側（Google Apps Script に貼るコード） |
| `assets/` | 画像とスプライト共通部品。2つのゲームで共有（約50MB） |
| `docs/` | 手順・工夫・改善案のメモ |

ゲームは1ファイルにHTML・CSS・JSを全部入れる形。ビルドや npm は使わない。

## 作業の流れ

同じファイルを複数のPC・複数のセッションが触るので、順番を必ず守る。

1. **始める前に `git pull`**（これを飛ばすと、他のPCの変更を巻き戻してしまう）
2. 直す前に、そのファイルをもう一度読む。**丸ごと上書きはしない**。必要な箇所だけ置き換える
3. ローカルで動かして確認（下記）
4. 区切りがついたら commit して **すぐ push**。手元に長く抱えない

同じ時間帯に別のセッションが同じファイルを触っていることがある。触る前に一声かけ、終わったら結果を伝える。

## 動かして確認する

`.claude/launch.json` に開発サーバーの設定がある（`python -m http.server 8797`）。Claude Code なら「アプリを起動して」で立ち上がる。手で動かすなら:

```bash
python -m http.server 8797
```

確認すること。

- **幅320px（iPhone SE）と390px** で、文字や数字が画面からはみ出さないか
- ブラウザのコンソールに JavaScript のエラーが出ていないか
- 背の低い画面（高さ500pxのiPad埋め込みなど）で、タイトルのボタンが1画面に収まるか

## 表示の決まりごと

- **数字**：各ゲームの `fmt()` が100万以上を「147万」「1.23億」「1.5兆」に縮める。画面の数字はすべてここを通す
- **結果画面の数字**：折り返さず `fitNums()` が枠に入るまで文字を小さくする（`.stat .v` は nowrap）
- **説明は出したときに1回だけ**：タイトルはキャッチコピーとボタンのみ。ルール一覧は「遊び方」パネル（`#ovHowto`）。プレイ中は指のお手本（`#finger`）と、初めて出たものへのひとこと（`#tip`）。見た項目は `gr.tips` / `mm.tips` に記録して二度目は出さない
- **2つのゲームともマップ方式**（Million March は `mm.map`、最後のフロアは「果てなき遠征」＝エンドレス）。解放したフロアだけ見せ、その上は雲の層（`assets/backgrounds/map_unknown.webp`）。新しいフロアが開くと雲が晴れる演出。マップの道を描く関数は `drawMapRoad`（ゲームの石畳の `drawRoad` と同じ名前にすると床が消える）
- **迎撃ロードはマップ方式**：タイトルの「出撃」でマップ（`#ovMap`）へ。どの面も**所持兵力・所持武器**（`MAP.army`・`MAP.power`）で出撃し、クリアしたときの兵力が次の所持兵力になる。失敗しても所持兵力は減らない（ライフはない）。クリア済みの面の遊び直しは×2なし・＋の板だけ（`isReplay()`）。武器は面ごとに上限があり（`weaponCap`：1面は弓…10面でロケット砲）、強い武器を持っていても、その面では上限の武器で戦う。3・6・9面のあとの宝物庫は1回だけ遊べ、手に入れたぶんは所持兵力に入る。進み具合は `gr.map`
- **迎撃ロードの難しさ**：敵の数・門や台座の固さ・増える板の大きさは、自分の兵力ではなく**その面の標準**（`refArmy`＝兵力の目安の曲線 `armyTarget`、`refPower`＝その面の標準の武器）で決める。だから兵が多いほど楽で、苦しいときは前の面で増員してから挑める。赤軍を受け止めずに通したときの被害は `PASS_LOSS`（敵の戦力の52%）。自動プレイでは、赤軍の3割をよけてしまう人でも「失敗したら前の面を2回遊び直す」で全10面に届く
- **Million March の味方の見た目**：兵が増えるとまとめて描く段が上がり、見た目が進化する（隊長→重装騎士→騎馬隊→聖騎士→巨大騎士→黄金の巨神。`ALLY_RANK_SPRITE`）。絵は `assets/evolve/`（ChatGPTで制作）。大軍で1色の塊に見えないよう、段階ごとに配色を変えた絵（騎馬=くすんだ緑、聖騎士=真紅、巨神=紫）に細い縁取りを焼き込んである。色のオーラは露骨に見えたのでやめた。描くときは上位の兵に1つ下・2つ下の兵を混ぜ（`composition`）、最前列は一番上の段の兵、その後ろは段ごとに均等に散らす（`placeCrowd`）
- **BGM**：`Snd` の中の `SONGS` に景色ごとの曲。ステージ開始時に `Snd.song(景色の番号)`、ボーナスは `Snd.song("bonus")`
- ファイル名・フォルダ名に空白を入れない（区切りは `_`）

## ランキング

Google Apps Script（`ranking-gas.js`）＋ スプレッドシート「広告ゲーム_ランキング」。オーナーのGoogleアカウントで動いている。

- ゲーム側の `RANKING_API` に、デプロイ済みのウェブアプリURLが入っている（2つのゲームで同じURL・`game` で区別）
- `stage` の意味：1〜10＝その面で力尽きた／11＝全10面制覇（またはエクストラ・エンドレス1面目で力尽きた）／12以降＝その先の面
- ランキングは「今週」（日本時間の月曜0時〜）と「今月」（1日〜）の2つ。どちらも1人1件のベスト記録で比べる（以前の「今日」タブ・今月のポイント制はやめた）
- 順位は「到達ステージ → 最高兵力 → 撃破数」の順。兵力だけで比べると新しい人が追いつけなくなるため
- `ranking-gas.js` を直したら、Apps Script に貼り直して「デプロイを管理 → 編集 → 新バージョン」で再デプロイする（URLは変わらない）。**デプロイと承認はオーナーが押す**
- 動作テストで入れた行は、スプレッドシートから消しておく

## やらないこと

- `.claude/` はコミットしない（`launch.json` だけ例外）
- 作業ログ・完了タスクをこのファイルに書かない（メモは `docs/` へ）
- ゲームに外部ライブラリを足さない（1ファイルで動く形を保つ）
